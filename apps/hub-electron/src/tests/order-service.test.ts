// OrderService coverage lives in focused files:
// order-service-kot, billing, history, alcohol, and transfer.
import { describe, it } from "vitest";

describe.skip("OrderService split test index", () => {
  it("is covered by focused order-service test files", () => {});
});
